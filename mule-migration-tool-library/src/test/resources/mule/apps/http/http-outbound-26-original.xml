<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <flow name="flow">
        <http:outbound-endpoint host="localhost" port="${httpPort1}" path="/backend" exchange-pattern="request-response" method="#[method]"/>
        <http:outbound-endpoint host="localhost" port="${httpPort2}" path="/backend" exchange-pattern="request-response" method="#[method]"/>
        <http:outbound-endpoint host="remotehost" port="${httpPort1}" path="/backend" exchange-pattern="request-response" method="#[method]"/>
    </flow>

</mule>
